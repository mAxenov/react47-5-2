import React from 'react';

function Card(children, props) { // передается заголовок и массив объектов для карточки с иконкой(если есть) наименованием и ссылкой
    return (
        { children } // если в заголовке есть картинка
        // с помощью компонента newsItem выводим элементы карточки
    );
}

export default Card; children, props