import React from 'react';

function SearchLine(children, props, search) {
    return (
        { children } // фото поисковика
        // тело поисковой строки и кнопка
    );
}

export default SearchLine;