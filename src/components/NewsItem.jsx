import React from 'react';

function NewsItem(children, props) {  //массив объектов для новости с иконкой(если есть) наименованием и ссылкой
    return (
        { chilred } // картинка из props
        //тело новости
    );
}

export default NewsItem;